{
  "name": "feedback-relay-worker",
  "version": "1.0.0",
  "description": "Tencent Cloud SCF primary feedback relay with Cloudflare Worker backup",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node scripts/test-relay.js",
    "tail": "wrangler tail",
    "init": "node scripts/init.js"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
