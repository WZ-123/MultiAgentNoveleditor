# 小说聊天读写链路审计

**最新结论见本文末尾“复审与解决方案”。** 以下首次审计和测试通过记录作为历史证据保留；不代表本轮重跑，也不代表已完成修复。

日期：2026-09-22。范围：当前工作区的聊天入口、项目/资源绑定、原生线程与工具配置、确认、提交、结果反馈和跨回合状态。未修改业务代码，未访问真实小说内容，未请求真实模型生成。

结论：确认 11 项独立缺陷，其中 9 项有生产函数或组件逻辑隔离复现，2 项由代码路径确认。另列 3 项待进一步验证/设计风险，不计入缺陷数量。不能由本次结果推算全系统缺陷总数。

验证方法：probe.cjs 调用生产服务方法，使用临时小说数据、替代传输和确定性 React hook 逻辑桩；它不等同于浏览器交互测试。probe-results.json 为运行结果。另运行现有 8 个测试文件和 Vite 构建。当前 Node 为 v25.8.0，项目声明版本为 24.19.0。本次未做真实 UI 截图验收，也未重放截图对应的真实会话。

## 已确认问题

| 编号 | 优先级 | 问题与触发条件 | 结果 | 证据 |
|---|---|---|---|---|
| 1 | P1 | 有小说项目，但无 Skill、resourceRef、chapterFileName 的普通聊天 | 明确提出修改也不挂载小说 MCP、不建立镜像；后端不考虑自然语言请求。由有工具页切到无资源页，还会切换原生线程模式 | 隔离复现；codexSessionService.js:453、471、474；App.jsx:1869 |
| 2 | P1 | 状态条仅使用模型 toolStatus | 模型通过过工具验证，就显示“可操作小说”，即使本回合处于纯文本模式；空状态提示也承诺会自行选择 Skills/小说工具 | 静态确认；AiChatPanel.jsx:220、238 |
| 3 | P1 | 打开章节但不选文字，selectionStart=selectionEnd=0 | 后端创建非空 selectionState，整章替换被当成越过选区拒绝。前端 hasSelection 要求 end>start，后端却接受 end>=start，契约不一致 | 隔离复现拒绝“补丁修改了当前选区之后的正文”；codexSessionService.js:483–503；nativeChangePolicy.js:127；App.jsx:1001 |
| 4 | P1 | 在聊天面板保留 activeId 时切换小说 A→B | 列表变了但 activeId 不重置，发送旧对话 A + 项目 B；服务未校验 history.novelId 与 payload.novelId。可能把 A 的上下文带入 B 的资源操作 | 前端逻辑及后端均复现；AiChatPanel.jsx:86、190；codexSessionService.js:459–462 |
| 5 | P1 | 回合运行/等待批准时切换或新建对话 | run、streamText、items、confirmation 为面板共用状态，切换不会清理或按会话恢复；旧确认可显示在新对话，旧回合终止事件又被 activeId 过滤，run 可滞留、发送受阻 | 状态保留隔离复现，终止事件过滤静态确认；AiChatPanel.jsx:128–148、163–169、210 |
| 6 | P1 | 通过编辑器操作生成待发提示，再切章节/改变选区后发送 | 排队内容只保存 text、skillName，没有冻结资源、版本和选区。提示仍指向旧章节，发送携带的却是当前 editorContext，导致范围矛盾、误拒绝或错误目标风险 | 静态确认；App.jsx:1654–1660；AiChatPanel.jsx:104–107、190 |
| 7 | P2 | 普通自然语言要求写作/修改，未由前端显式传 Skill | operation 一直 unspecified，哪怕有章节工具也不建立写作进度。上一回合用 Skill 后 queuedSkill 被清空，普通“继续”失去这套任务语义 | 隔离复现 operation=unspecified、progress=null；writingTaskConstraints.mjs:1；codexSessionService.js:494–510；AiChatPanel.jsx:191 |
| 8 | P1 | 正常完成后需要核查是否实际写入 | UI 丢弃 taskResult/savedResources，清空实时 items；历史只展示 message.text。持久化 toolCalls 的过滤还排除了 fileChange，且没有保存 taskResult。聊天无法用正式提交证据约束模型的“已写入/被拒绝”叙述 | 完成消息丢失 fileChange/taskResult 的隔离复现；codexSessionService.js:1072–1094；AiChatPanel.jsx:139–146、239–251。宿主 ledger 尚保留证据，不是所有日志都丢失 |
| 9 | P1 | 已流式显示文字后停止或发生失败 | 仅 turn_completed 保存 assistant；interrupted/failed 不保存已有文本，前端随后清空 streamText。部分已提交的修改不会因此回滚，但聊天中的过程说明消失 | 两种终态均隔离复现 assistantStored=false；codexSessionService.js:1072；AiChatPanel.jsx:139–146 |
| 10 | P2 | 发送前保存失败，或创建对话/追加消息失败 | 这些 await 在 try 之外；click/keydown 也没有捕获 send 的拒绝。前置失败绕过聊天错误提示。保存错误可能另有编辑器状态，但聊天缺少解释 | 保存失败隔离复现为 rejected promise，聊天 error 仍为空；AiChatPanel.jsx:171–194 |
| 11 | P1 | 同一对话快速重复发送/并发 startTurn | 前端 await 保存与消息落盘后才 setRun，后端检查 threadRuns 后又经过多个 await 才登记，存在检查与占用间隙。无资源普通聊天同一对话可同时接受两个 run | 两次并发 startTurn 均成功，activeRuns=2；codexSessionService.js:444、512–513；AiChatPanel.jsx:171–187。未声称复现两个小说写入事务同时提交 |

P1 表示应优先修复的功能正确性/上下文隔离问题；此审计没有证据表明用户真实小说已被误改。

## 未计入数量的风险

1. 异步加载旧结果覆盖新状态：loadThreads、loadActive、授权/进度加载没有取消或请求版本校验。快速切换时存在乱序回写风险，未做延迟浏览器测试。
2. 工具失败恢复政策：任意 item_completed.status=failed 都触发整回合 interrupt。对错误资源参数、可重试工具失败等是否应允许模型纠正，需要区分错误类型并做真实运行时测试，不能一概认定为故障。
3. 完成语义：现有测试明确允许字数未达目标但 goalVerified=true；review/read 等路径也缺少任务特定证据门槛。这是当前设计选择与“目标已完成”文案之间的风险，不是本次新增回归结论。

## 通过的验证与正常环节

- task-ledger-resource-contract.test.js：通过。
- native-novel-workspace.test.js：通过，映射、差异检测和未知文件阻断。
- native-patch-read-after-write.test.js：通过。
- novel-mcp-mutation.test.js：通过，资源写入事务相关检查。
- writing-authorization-policy.test.js：通过，默认确认、追加授权、拒绝和范围相关策略。
- codex-native-cutover.test.js：通过，使用本机模拟 Responses 服务验证原生线程、文本/工具模式切换等。
- codex-native-apply-patch.test.js：通过 deepseek-v4-flash、deepseek-flash、deepseek-v4-pro 三种模型标识的模拟链路；并非向真实 DeepSeek 服务发请求。
- codex-continuous-writing.test.js：通过，模拟服务驱动同回合逐章保存。
- Vite production build：通过，无构建警告，输出至 /private/tmp/mana-chain-audit-build。
- 原生 MCP 本身会返回 workspacePath/hash，空编辑器 baseHash 不意味着用户必须手填版本。allowedWriteResourceRefs 为空时也不是全面禁止写入；写入仍受批准、范围、差异与提交校验控制。
- 原生补丁提交会校验批准内容与实际变更；应用有提交事件刷新小说/资料页面的代码。不能把截图现象归结为“底层完全不能保存”。

最初 3 项集成测试因沙箱禁止监听 127.0.0.1 报 EPERM；获准在可监听本机端口的环境重跑后全部通过。这是测试环境限制，不计为产品问题。

## 修复顺序建议（本次未实施）

1. 先封住项目/对话错配、会话状态串用、并发启动间隙；绑定不可变的任务目标和选区。
2. 统一实际会话能力与界面状态，消除通过“当前打开哪个页面”意外决定能否操作项目的行为。
3. 修复光标与非空选区的区别；使普通聊天与后续“继续”的任务语义可持续。
4. 保存并展示宿主终态结果、原生 fileChange 和部分提交证据；保留失败/中断回复并兜住发送前错误。
5. 把本次反例加入从真实聊天入口发起的用户流程测试，而不只由测试直接传入完整 Skill 和 editorContext。

## 复审与解决方案

依据：2026-09-22 最新 [AGENTS.md](../../AGENTS.md) 和 [统一工作流](../../knowledge-base/ux-dev-flow.md)。本节是修复计划，不是实现完成声明。业务源码未修改；本轮只更新审计记录、基线及复审证据，并在知识库缺陷清单增加入口。

### 当前结论与证据边界

11 项源码问题仍成立，但不是 11 个互不相关的根因，归为下面 4 组。重新运行原探针，反例断言均成立；产出 11 条观察记录，不能把观察条数等同于 11 项缺陷都做过独立 UI 复现。

- 当前证据：[复审探针结果](review-probe-results.json)、[时间/环境/源码 hash 基线](review-baseline.json)。工作区已有大量未提交变更，本轮未改动这些业务文件，也不能用 HEAD 代表实际审查版本。
- KB-01、03、07、08、09、11 有生产方法及隔离存储验证；KB-04 有宿主校验与组件发送逻辑验证；KB-05、10 有组件逻辑验证；KB-02、06 为源码路径确认。
- 原组件探针以 hook 替身执行组件函数，useEffect 不运行，部分状态直接预置。它能帮助验证局部逻辑，不能证明 React 生命周期、迟到事件和真实点击的全部后果。上述 UI 结论仍需实际入口验收。
- KB-07 限定为“宿主任务分类和进度缺失”；不能据此说模型一定忘记前文，也不能简单永久继承上一次 Skill/选区。
- KB-08 是聊天结果投射缺失，不是所有日志丢失；ledger 仍记录正式提交。KB-09 的已显示文本可能消失，不意味着已正式保存的小说被撤销。
- KB-11 证明两个普通聊天 start 被宿主接受，不证明原生 App Server 执行了两个回合，更不证明同时提交了两个小说写入事务。
- 本轮重跑 writing-authorization-policy、native-novel-workspace、native-patch-read-after-write，均通过。未重跑上文的 8 项全部测试、构建、真实模型或 UI，不继承其通过结论。

### 根因一：运行归属由面板临时状态代管，发送又缺少完整的启动边界

对应 KB-04、05、10、11。AiChatPanel 把当前显示的对话与当前执行的任务混为一谈；后端虽有运行表，却未在异步准备前完成占位，也未校验历史所属项目。发送前保存、消息落盘和运行启动分散处理，错误和重试缺少统一归属。

| ID | 具体修法（待实施） | 必须检查的关联点 |
| --- | --- | --- |
| KB-04 | 当前项目改变时选择该项目的对话，草稿按项目/对话保存；异步加载带请求版本，拒收旧结果。宿主在绑定原生线程/创建镜像前验证 history.novelId 与请求一致，并校验 userMessageId 属于该对话；不偷偷移动对话 | 切换项目、新建/删除对话、历史分支、LAN 客户端；发起后切页面不改变运行目标 |
| KB-05 | 使用宿主的运行/批准快照作为事实源，UI 按 conversationId/runId 展示。切换只改变视图，后台事件继续归档。增加有版本的快照读取，订阅事件后合并快照，避免丢事件/旧快照覆盖新事件 | preload、IPC、LAN API 同步；确认和停止绑定具体 run，旧确认不可作用新运行；返回对话能恢复等待批准状态 |
| KB-10 | 从发送前保存开始包住整个准备过程；保留原文和已捕获目标。区分“消息未保存”“已保存但未启动”“启动结果未知”。重试复用已落盘消息，不重复追加；请求结果未知先查询运行状态 | 编辑器保存冲突、存储失败、主进程启动失败、网络断连、重复回调；失败不得拿旧磁盘正文继续执行 |
| KB-11 | UI 在第一个 await 前同步占位。宿主同步预留 run/conversation，再进行异步验证和启动；项目镜像锁在同步镜像前取得，不能仅靠按钮禁用。重复请求使用稳定 request/run 身份；所有失败出口按占位所有者释放 | 启动失败重试、取消、模型切换、编辑/回退/删除历史、配额清理；旧 finally 不得释放新任务占位 |

先在现有 CodexSessionService 上明确 starting/running/terminal 生命周期，不新建另一套调度器。启动结果未知时避免再次请求模型。保留现有单项目镜像运行约束；运行中的历史删除/配额清理必须跳过或明确拒绝，不能删除任务正在使用的记录。确认快照可以供切换视图恢复，进程重启后旧批准只能显示失效/中断，不自动续批。

### 根因二：编辑器上下文既被当作提示，又被当成硬性修改范围

对应 KB-03、06。普通光标、非空选择、编辑器命令指定的范围没有一致的语义；命令文本和实际请求目标在不同时间读取。

| ID | 具体修法（待实施） | 必须检查的关联点 |
| --- | --- | --- |
| KB-03 | 区分上下文提示和明确修改范围。普通光标不产生选区锁；明确的非空 selection 才检查选区外不变；显式“在光标插入”可保留零长度 insertion 范围。前后端使用一致校验 | 普通整章修改、仅改选区、光标插入、选区失效、跨资源补丁、批准后内容变化；不得删除现有选区保护 |
| KB-06 | 编辑器命令排队时保存项目、资源、操作类型、范围和对应版本，UI 显示命令目标；发送前保存该目标并核验同一内容版本。切章节不替换目标，切项目将旧命令保留在原项目而不混发 | 保存队列必须按目标资源操作，不能只 flush 当前打开章节；延迟发送、编辑器脏稿、项目切换、重试和旧版本 |

保存后的新版本若与命令选区内容不一致，先阻断这次局部编辑并提示重新选择，不能在新内容上套用旧偏移。只冻结用户明确指定的范围，不冻结整个小说上下文；模型仍通过工具读取最新资料。用户明确请求更大范围时，需要可见地更新任务范围，而不是暗中越过原保护。

### 根因三：模型能力、项目工具挂载和写作进度被 Skill/编辑器页面间接决定

对应 KB-01、02、07。模型工具验证、当前项目上下文、挂载模式和 operation 是四个不同概念，却分别用页面状态和 Skill 名推断。

| ID | 具体修法（待实施） | 必须检查的关联点 |
| --- | --- | --- |
| KB-01 | 为项目聊天确定稳定的项目工具模式：绑定有效项目且模型工具已验证时，挂载该项目读取工具和受控补丁环境，不因打开资料页/设置页变化。无项目聊天保留文本模式；工具不可用时明确标明仅文本，不虚构操作能力 | 模型验证、订阅/API、旧 thread 指纹和一次性历史导入、项目镜像互斥、内置 Skill；模型探针和导入 one-shot 不得被全局改成可写项目任务 |
| KB-02 | 由宿主返回配置能力和本回合实际能力，UI 区分准备中、项目工具可用、仅文本及挂载失败；批准状态单独显示。未启动时不假称本回合已挂载 | 模型中心、切换模型/推理档位、连接失效、启动失败和页面切换；probe 成功不替代补丁验收 |
| KB-07 | 保存统计由运行及真实 commit 驱动，不能只有 operation=write/edit 才记录。Skill 只辅助任务，不作执行事实来源。普通后续请求在该会话中继续，新的目标/范围来自本轮明确输入，不能盲目继承旧选区 | writingProgress、taskLedger、report_writing_progress、结构化资料修改、连续保存、终态结果；不能将模型结束或有提交等同用户目标达成 |

这不是增加自然语言关键词分类器。允许项目工具不等于允许免确认写入；保持原生 apply_patch、宿主批准、范围/版本校验和新增正文限定授权。现有镜像按项目目录重建，不能为了“所有聊天有工具”同时放开同项目并行运行；如需优化读任务并行，应另做受控设计。本批先正确再评估开销。

### 根因四：宿主执行证据与聊天消息是两条没有完整衔接的数据路径

对应 KB-08、09。宿主有事件、批准和提交信息，聊天只持久化正常结束的模型文本；终态 UI 又丢弃流与工具列表。模型叙述因此替代了事实反馈。

| ID | 具体修法（待实施） | 必须检查的关联点 |
| --- | --- | --- |
| KB-08 | 用现有运行记录生成统一终态结果，计算后持久化到对应聊天回合，再通知 UI。按 itemId 更新工具记录，保留 fileChange，并区分原生工具完成与宿主正式提交。UI 展示已保存资源/拒绝/失败/未验证目标，可重开查看 | chatHistory、ledger、IPC、LAN、确认、部分提交、资料刷新；历史旧消息显示“无执行证据”，不能补造已保存结论 |
| KB-09 | 正常、失败、取消、运行时退出均走统一收尾；保留已有文本、终态、错误和 savedResources。以稳定 run/attempt/message 身份幂等保存；没有 turnId 的启动失败也可定位。为进程意外退出保留周期性检查点和提交证据 | _onNotification、_failAll、dispose、历史 ensureRuntimeMessage、重启恢复；意外崩溃只能保证最后已持久化点，不承诺未落盘的最后几个字 |

终态写入失败不能先显示“已完成”；应保留可重试收尾记录并使用已有 ledger 的 commit 证据核对，避免重试导致重复小说提交。已有 ensureRuntimeMessage 对重复消息文本有校验，不能直接复用固定 codexTurnId 来反复写检查点；需要支持同一运行的检查点更新与终态封存。保存事件是事实来源，资料面板的刷新应消费项目和资源标识，不依赖模型选择了哪个工具名字。刷新不得覆盖用户尚未处理的脏稿。

### 关联检查发现：不能只改聊天组件

| 关联系统 | 本轮看到的事实 | 纳入计划的处理 |
| --- | --- | --- |
| 桌面/LAN 桥接 | preload.js 与 remoteServer.js 分别维护 codex API；LAN 版本当前未提供聊天组件调用的 getWritingAuthorization/getWritingProgress 等方法 | 源码接口缺口已确认，远程实际表现待验证；新增快照/API 时同时对齐受支持方法和既有远程策略，不扩大远程敏感操作权限 |
| 资料页刷新 | DataTabContent/NovelDataBrowser 根据工具名刷新，App 根据 committedResources 刷新；存在不同事件口径 | 核对原生 fileChange 后已打开的人物/世界/大纲页是否刷新；不凭 App 刷新代码就宣称所有资料页正常 |
| 现有 UI 测试 | codex-native-ui-e2e.js 等待“Codex 原生线程”，当前状态标签已不输出该字样；写作用例使用 sendWithSkill | 测试前置条件源码已过时，尚未运行判为超时；先改为稳定状态断言，再补无 Skill 的真实聊天入口。保留显式 Skill 场景，不能用它证明普通聊天可用 |
| 历史/模型/导入 | 编辑与回退依赖 threadRuns；archive 删除和配额另有入口；模型探针与导入通过同一服务运行 | 新增 starting 占位、收尾和项目模式时都要检查这些消费者。不得把导入 one-shot/模型探针误归到用户聊天，也不能让旧终态释放新运行 |

这些是关联范围及补充发现，不混入原来的 11 个编号，也不据此断言导入等全部系统已坏。

### 实施批次与验收清单（尚未执行）

先修测试前置条件，复用现有 Electron UI + 本机 Fake Responses 脚本，增加可控延迟、等待批准、失败和多项目夹具。从真实页面点击发送；后端直接注参的负向校验另测。客户端全程静音，测试数据隔离。此步骤完成只代表验收入口可用，不代表业务修复。

| 批次 | 修复范围 | 本批必须通过的用户/关联检查 |
| --- | --- | --- |
| A1 | KB-04、10、11：请求身份、发送准备、启动占位 | 小说 A→B→A；保存失败不启动；消息落盘后启动失败可重试且不重复；双击/两个客户端同时发送只接受一个；模型启动失败后占位可释放；编辑/删除/配额不得破坏 starting/running |
| A2（依赖 A1） | KB-05：快照、事件归属与确认恢复 | A 等待批准时切 B，B 不显示 A 的确认；A 后台完成后切回显示终态；旧事件迟到不覆盖新运行；断连重连可恢复；重启不复用旧批准；桌面与允许的 LAN 路径一致 |
| B（依赖 A1/A2 的任务身份） | KB-03、06：稳定目标与范围 | 无选择整章改写成功；明确选区越界失败；光标插入有效；排队后切章节仍指向原目标；内容版本改变时旧选区不执行；批准后差异变动仍被阻断 |
| C（依赖 A/B） | KB-01、02、07：项目能力与保存进度 | 从资料页自然语言要求修改人物卡，不注入 Skill；切章节/设置页能力不变；工具不可用时准确显示；后续“继续”有真实保存统计且不继承旧限制；探针、导入、旧会话恢复不回归 |
| D（依赖 A2/C，收尾设计在 A 阶段预留） | KB-08、09：证据、失败与持久化 | 模型声称写入但无提交时无“已保存”；确认后读回并显示资源；先保存一处再失败，历史明确部分保存；停止保留文字；重开历史结果一致；资料页刷新而不覆盖脏稿；重复终态不产生重复消息/提交 |

每批运行相关局部/集成检查与受影响 UI 场景。最后合跑：从资料页修改 → 确认 → 正式读回 → 检查资料页 → 切对话/项目 → 返回 → 继续 → 部分保存后停止 → 重启核查。成功、拒绝、冲突路径都要有证据。模拟结果通过后再做小规模真实模型验证，不把两者混写；发布构建不在本次审查范围。

### 本轮交接

| 问题与根因 | 本批改动 | 验证入口、环境和结果 | 未验证项 | 下一步 |
| --- | --- | --- | --- | --- |
| 11 项归并为 4 组根因，关联到桥接、历史、模型、资料刷新 | 只补充本报告和知识库索引，生成复审证据；没有修业务代码 | Node v25.8.0；原隔离探针反例仍成立；3 项存储/授权守卫测试通过；详见 review-baseline.json | 真实 UI、真实模型、完整回归、各批解决方案的运行结果 | 从测试前置条件与 A1 开始；所有缺陷仍保持未修复状态 |


## 实施记录（2026-09-22）

本节是用户批准计划后的实际实施结果。上方“尚未执行 / 未修复”属于此前审计时点，不代表本节实施后的状态。保留工作区原有改动，未发布、未部署，也没有新建另一套 agent 执行链。

### 本轮改动与证据

| 批次 | 根因及改动 | 本轮验证 | 状态 |
| --- | --- | --- | --- |
| 0 | 修正旧 UI 状态文案断言；状态属性、两本隔离小说、模拟 Responses 延迟与补丁场景；普通需求从输入框发送 | Electron 两阶段运行；所有测试客户端静音 | 完成，单独不算业务修复 |
| A1（KB-04/10/11） | startTurn 接管用户消息入库；同步占位、请求指纹、对话/项目/消息归属、原消息重试；历史删除/移动/配额遵守运行占位 | chat-chain-contract：错配不调模型、相同请求仅一次、内容冲突拒绝、消息失败/启动失败释放、重试不重复消息；chat-chain-lan：两个 HTTP 客户端并发仅一个运行 | 通过列明检查 |
| A2（KB-05） | 会话/运行快照、身份与版本合并、按项目保存对话和草稿、恢复代次、批准绑定运行；切换不取消后台任务 | UI：确认时重载、A→B→A、草稿恢复；契约：迟到事件不覆盖新状态、崩溃检查点恢复中断、旧确认不可执行 | 通过列明检查 |
| B（KB-03/06） | 普通选区为上下文；显式 selection/insertion 绑定资源/版本/范围；保存队列按目标章节；宿主强制资源白名单和前后文保护 | 实际编辑器菜单：排队后切章仍修改原章、插入保留两侧正文、修改正文后旧范围拒绝；批准内容哈希及越界守卫测试 | 通过列明检查 |
| C（KB-01/02/07） | 项目聊天按项目和模型验证挂载工具；准备/实际能力/批准分开；所有项目工具任务有活动及保存统计，one-shot/探针保持原工具配置 | 普通输入从人物卡/世界观页发起修改、确认、正式读回及刷新；工具失败模型仍可纯文本；原生 cutover 与模型验证测试 | 通过列明检查 |
| D（KB-08/09） | 按 itemId 更新并保留 fileChange；运行消息稳定 ID、每两秒文字检查点、确认/提交/终态立即存；统一收尾，存储失败重试收尾；旧历史不补造证据 | UI：拒绝不写、停止保留文字、保存一处后停止仍保留正式内容、重启历史继续；契约：终态存储失败不重跑模型、不重复消息；资料脏稿保留 | 通过列明检查 |

### 关联检查发现并补修

1. **原生补丁格式衔接**：批准事件可带无行号 `@@`，完成事件转换为带行号 unified diff。原校验误拒合法补丁。现在只接受能唯一精确定位的上下文块；批准与完成按资源、操作、before/after 哈希核对，正式镜像内容再核对一次。重复上下文、无定位、越界和内容改变仍拒绝。
2. **编辑器被刷新清空**：全量章节树刷新重新生成章节 ID，导致打开的 tab 失去章节。实际截图发现后修复为保留章节身份、刷新已加载正文、忽略迟到的树快照并保留脏稿。最终 UI 断言保存后编辑器仍显示最新正文。
3. **资料刷新口径**：人物卡、世界观等统一按正式提交资源类型刷新。世界观有未保存草稿时显示冲突并禁用覆盖保存；取消编辑后读回最新版本。大纲加载绑定请求的小说，不从后来切换的全局 active 项目取路径。
4. **收尾与恢复边界**：正式事务成功即保留提交证据，后续读回失败也不丢失“已保存”事实；恢复只修补缺失/未完成记录，不批量改写旧历史。generation 防止远端页面的未持久化旧版本挡住宿主恢复状态。
5. **测试自身故障**：导入原子回滚测试依赖 chmod 制造写失败，在本运行环境中没有触发失败。改为对目标世界观正式写入注入一次 EACCES，验证前面已处理资源回滚及 staging 不标 merged；没有修改导入业务逻辑。

6. **连续补丁竞态**：最终复跑发现模型已进入下一个补丁时，宿主可能还在提交上一个补丁，镜像收集会混入后续变更。原生批准入口现在等待先前通知/提交处理完；连续创作测试刻意延迟宿主检查点，验证三次逐章提交不混合。该问题来自真实最终回归失败，不能由早先通过结果掩盖。

### 当前验收证据

| 层次 | 命令 / 入口 | 结果与证据 |
| --- | --- | --- |
| 主测试 | `node scripts/run-unit-tests.js` | 20 个测试文件通过，含模型配置/验证、原生读写、范围保护、运行身份与 LAN；[日志](unit-results.log) |
| 前端构建 | `npx vite build` | 通过；[日志](build-results.log) |
| 完整模拟 UI | `node scripts/run-codex-native-ui-e2e.js` | 两阶段静音 Electron；[日志](ui-results.log)。测试通过普通输入和实际编辑器菜单，不替普通聊天注入 Skill/资源参数 |
| LAN | `node test/chat-chain-lan.test.js`；`node test/lan-remote-dev-origin-regression.test.js` | 认证、桥接方法、并发、查询、远程创建限制及页面入口通过。模型执行在 LAN 测试中为替身，真正原生调用由其他集成/UI 测试验证 |
| 导入关联 | import-saga、import-atomic-boundary、import-merge-reset 三个 regression | 通过；[saga](import-saga-results.log)、[回滚](import-atomic-results.log)、[重置](import-reset-results.log) |
| 真实模型 | `node scripts/check-chat-chain-live.js` | 已配置 `deepseek-flash`：真实工具读取、一次确认修改、一次拒绝；正文读回符合结果，3 项通过。[记录](live-results.json)。临时小说已清理，原模型配置未变 |

界面截图已由 Codex 打开复核。保存一处后停止、世界观冲突、人物卡刷新和插入后的正文均在画面中可见；未以截图文件存在代替检查。

- [人物卡正式提交后刷新](../../qa-screenshots/chat-chain-character-refreshed.png)
- [世界观正式提交后刷新](../../qa-screenshots/chat-chain-world-refreshed.png)
- [世界观脏稿保留及冲突提示](../../qa-screenshots/chat-chain-world-dirty-conflict.png)
- [切回项目恢复确认](../../qa-screenshots/chat-chain-restored-approval.png)
- [停止后保留文字](../../qa-screenshots/chat-chain-stopped-history.png)
- [部分保存后停止](../../qa-screenshots/chat-chain-partial-save-stopped.png)
- [切章后原选区目标仍正确](../../qa-screenshots/chat-chain-bound-selection.png)
- [插入后编辑器仍显示最新正文](../../qa-screenshots/chat-chain-insertion.png)
- [旧版本范围拒绝](../../qa-screenshots/chat-chain-stale-selection.png)

### 验收边界与后续

本轮 11 项已实现，并通过上述局部、关联系统、模拟 UI 与小规模真实模型检查。未做安装包/跨平台/部署验收；真实模型仅覆盖记录中的已配置模型，不推广为所有供应商均通过。崩溃恢复使用持久化检查点故障夹具验证，未做物理断电测试；未落盘字符不承诺恢复。LAN 并发为两个实际 HTTP 客户端，未做远程手机浏览器长时间断网测试。导入检查覆盖受影响存储边界，不等于重新验收整套联网角色补全和大规模导入。

后续修改继续运行本轮保留的测试入口，尤其保留“提交后当前编辑器还在且内容正确”的检查；不能只凭聊天成功标签判断产品完成。原历史审计探针断言的是修复前反例，不作为当前应继续通过的测试。

## 补充：原生补丁确认卡差异着色（2026-09-22）

用户截图发现原生 `apply_patch` 的确认卡把完整 diff 当作统一灰色文本输出，`+` 与 `-` 只剩字符提示，无法直观看出新增和删除。本次仅调整聊天 UI：新增行以绿色、删除行以红色显示，hunk 标记为蓝色，上下文保留普通颜色；不改变补丁、确认、提交或正式资源读回逻辑。

回归入口在 `test/codex-native-ui-e2e.js`：等待真实确认卡同时存在新增/删除行，并断言两种行分别携带绿色/红色样式，再保存确认卡截图。

本次验证：`npx vite build` 通过；`npm test -- test:ui-e2e` 在静音 Electron 与隔离模拟 Responses 服务中两阶段通过。截图已打开肉眼复核：确认卡展开后，删除行清晰为红色背景与文字，新增行为绿色背景与文字，hunk 标记为蓝色，确认和拒绝按钮仍可见且未被遮挡。

- [原生补丁确认卡着色截图](../../qa-screenshots/codex-native-write-confirmation.png)


## 用户复现后的补修：已有人物卡更新（2026-09-22）

用户截图证明上一轮“人物卡写入已验收”的覆盖不够：当时 UI 测的是新增人物卡，真实模型测的是章节更新，没有测正式存储重新读取后已有 JSON 资源的更新。上一轮通过结果不能证明这一分支正确。

**实际根因已用该次运行的哈希验证**：正式人物卡序列化不带末尾换行，原生 apply_patch 更新文件时补入 LF。宿主的批准证据只对 chapter 资源预测这个 LF；character 的预测结果因此少一个字节。把失败记录中的 diff 应用到原内容，得到 expectedAfterHash；只追加一个 LF，就精确得到 actualAfterHash。差异不是用户缺权限，也不是人物字段值被偷偷改动。仅保存去内容的 [复现证据](character-update-reproduction.json)。

修复 `nativeChangePolicy.evidenceForNativeChanges`：所有受控资源统一按原生补丁的末尾换行行为计算候选结果，而不是仅处理章节。提交时仍严格比较批准候选、完成事件和实际内容哈希；没有 trim 实际内容、忽略空白或绕过批准。

新增 `native-existing-resource-update.test.js`，使用实际 Codex 原生 apply_patch + 模拟 Responses + 隔离正式存储，覆盖人物卡 role 更新、下一回合 personality 更新、无末尾换行世界观更新。该测试在修复前报同样的 approved_content_mismatch，修复后通过，并加入主测试。

UI 新增“先创建人物卡 → 下一回合普通输入修改已有性格 → 确认 → 正式读回 → 当前人物卡页面刷新”。修正显示断言不依赖冒号后的空格，自动化窗口关闭后台节流，避免被其他窗口遮挡时帧等待停滞；生产窗口仍使用原默认节流设置。

本次结果：

- [21 组主测试](character-update-unit.log)通过，包含批准内容不一致拒绝、选区保护、连续补丁及新人物卡更新回归。
- [前端构建](character-update-build.log)通过。
- [完整模拟 UI](character-update-ui.log)两阶段通过；[已有人物卡更新截图](../../qa-screenshots/chat-chain-existing-character-updated.png)已打开肉眼复核，页面为新性格，聊天记录为 character replace 的正式保存。
- [真实模型记录](character-update-live-results.json)：已配置 deepseek-flash 对隔离既有人物卡完成读取、role/personality 确认更新、拒绝后值不变，3 项通过。

运行入口：`node test/native-existing-resource-update.test.js`；`MANA_LIVE_CHARACTER_ONLY=1 node scripts/check-chat-chain-live.js`。真实模型仍不代表所有模型验收。修复落在本工作区源码，未重启用户当前应用进程、未发布安装包；当前进程需要重新加载宿主代码才能使用修复。
